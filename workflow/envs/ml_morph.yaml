name: ml_morph_env
channels:
  - conda-forge
dependencies:
    - python=3.10
    - pandas>=0.22.0
    - numpy<2 #this version is important to avoid binary incompatability
    - opencv>=3.4.0.12
    - dlib>=19.7.0